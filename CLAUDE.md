# Ukido AI Assistant v0.13.3 - MVP с планом критических исправлений

## ⚠️ ВАЖНО: Требуются критические фиксы перед production!
**См. файл:** `MVP_CRITICAL_FIXES_PLAN.md` - детальный план исправлений (7-9 часов работы)

### Самые критичные (5 минут работы):
1. **HTTP Timeout** - без этого сервер может зависнуть навсегда
2. **Проверка API ключа** - без этого запустится в нерабочем состоянии

## ⚡ Quick Start (30 секунд)

```bash
# 1. Запуск сервера
python src/main.py
# ✅ Должно быть: "🎭 Система юмора Жванецкого инициализирована (вероятность: 33.0%)"

# 2. Быстрый тест
python http_sandbox_universal.py dialog_v2_1  # Полный диалог
# или
curl -X POST http://localhost:8000/chat -H "Content-Type: application/json" \
  -d '{"user_id":"test","message":"Привет! Расскажите о школе"}'
```

### API Endpoints:
- `POST /chat` - основной endpoint
  - Request: `{"user_id": "string", "message": "string"}`
  - Response: `{"response": "string", "intent": "string", "user_signal": "string", "humor_generated": bool}`
- `GET /health` - проверка статуса сервера

## 🎯 О проекте

**Что:** AI-чатбот для детской онлайн-школы soft skills Ukido  
**Для кого:** Родители детей 7-14 лет из Украины  
**Формат:** 100% онлайн через Zoom, офис в Киеве только для консультаций  
**Ключ:** Адаптивные ответы под эмоциональное состояние родителя

## 🏗️ Архитектура Pipeline

```
USER MESSAGE → [ROUTER] → [ORCHESTRATOR] → [GENERATOR/HUMOR] → RESPONSE
```

### Детальный Data Flow:

#### 1️⃣ ROUTER (router.py) - Gemini 2.5 Flash
```
Входные данные: 
{
  "message": "Добрый день! Расскажите о школе",
  "user_id": "test_123",
  "history": [{"role": "user", "content": "..."}, ...]
}
↓
Процесс:
1. Анализ через Gemini с промптом 600+ строк
2. Поиск документов через trigger_words из summaries.json
3. Декомпозиция сложных вопросов на атомарные
↓
Возвращает JSON:
{
  "intent": "success",                               # success/offtopic/need_simplification
  "user_signal": "exploring_only",                   # 4 варианта сигналов
  "documents": ["methodology.md", "pricing.md"],     # до 4 документов
  "decomposed_questions": ["Расскажите о школе"],    # атомарные вопросы
  "social_context": "greeting",                      # greeting/thanks/farewell/apology/null
  "implicit_questions": []                           # только для ready_to_buy без вопросов
}
```

#### 2️⃣ ORCHESTRATOR (main.py)
```
Получает router_result (см. выше)
↓
Действия:
1. Сохраняет в history_manager.py:
   chat.history = OrderedDict({
     "user_123": deque([
       {"role": "user", "content": "..."},
       {"role": "assistant", "content": "..."}
     ], maxlen=10)  # LRU на 1000 пользователей
   })

2. HOTFIX (строки 159-179): 
   if intent == "offtopic" and user_id in chat.user_signals_history:
       router_result["user_signal"] = chat.user_signals_history[user_id]
   
3. Обновляет social_state.py:
   pure_social_states[user_id] = (social_context and not decomposed_questions)

4. filter_offtopic_from_history():
   - Удаляет сообщения где intent был "offtopic"
   - Оставляет только success для контекста Claude
   - Максимум 10 последних сообщений

5. Маршрутизация:
   if intent == "success":
       → response_generator.py с filtered_history
   elif intent == "offtopic":
       if not blocked and random() < 0.33:
           → zhvanetsky_humor.py
       else:
           → standard_responses.py
   else:
       → стандартный ответ "Упростите вопрос"
```

#### 3️⃣ GENERATOR (response_generator.py) - Claude 3.5 Haiku
```
Входные: 
- router_result (JSON из Router)
- filtered_history (без offtopic)
- current_message

↓
Процесс:
1. Загружает документы:
   for doc_name in router_result["documents"]:
       content = load_file(f"documents_compressed/{doc_name}")

2. Выбирает tone через tone_map:
   tone_map = {
       "ready_to_buy": "деловой, конкретный, побуждающий",
       "anxiety_about_child": "эмпатичный, поддерживающий",
       "price_sensitive": "подчеркивающий ценность",
       "exploring_only": "дружелюбный, информативный"
   }

3. Few-shot примеры (метод _get_few_shot_examples):
   - Берёт 2-3 примера из offers_catalog.py
   - Показывает как органично встраивать CTA
   - Адаптирует под текущий user_signal

4. Промпт для Claude:
   system_prompt + tone + few_shot + documents + questions

5. Постпроцессинг (строки 99-115):
   # Приветствие
   if social_context == "greeting" and not starts_with_greeting:
       text = "Привет! " + text
   
   # CTA интеграция
   if should_add_cta():  # rate limiting 1/2 сообщений
       text = integrate_cta(text, user_signal)
   
   # Чистка
   text = text.replace("Мы понимаем", "Многие родители")
   
Возвращает:
{
  "response": "Привет! Для 11-летних отлично подойдёт...",
  "intent": "success",
  "user_signal": "exploring_only",
  "humor_generated": false
}
```

#### 3️⃣ HUMOR (zhvanetsky_humor.py) - для offtopic
```
Входные:
- router_result с intent == "offtopic"
- user_id для rate limiting

↓
Проверки (все должны пройти):
1. check_user_signal():
   if user_signal in ["anxiety_about_child", "price_sensitive"]:
       return False  # Блокировка

2. is_pure_social():
   if pure_social_states.get(user_id, False):
       return False  # Чистое приветствие без вопросов

3. Rate limiting:
   user_humor_count[user_id] = {
       "count": 2,
       "last_reset": timestamp
   }
   if count >= 3:  # За последний час
       return False

4. Вероятность:
   if random.random() > 0.33:
       return False

↓
Если ВСЕ проверки пройдены:
   → Генерирует юмор в стиле Жванецкого через Claude
   → Возвращает {"response": "Шутка...", "humor_generated": true}

Иначе:
   → standard_responses.py возвращает случайную фразу:
     ["Это интересный вопрос, но давайте вернёмся к обучению",
      "Хороший вопрос! А что вас интересует про наши курсы?", ...]

## 📊 State Machine

### 4 User Signals (приоритет по убыванию):

| Signal | Триггеры | Блокировки | Tone | CTA |
|--------|----------|------------|------|-----|
| **ready_to_buy** | "запишите", < 5 слов | - | Деловой, конкретный | Высокий приоритет |
| **anxiety_about_child** | "боится", "плачет" | 🚫 Юмор | Эмпатичный, "Многие родители..." | Средний |
| **price_sensitive** | "дорого", жалобы на цену | 🚫 Юмор, инерция 85% | Подчёркивать ценность | Низкий |
| **exploring_only** | По умолчанию | - | Дружелюбный | Средний |

### Критические переходы сигналов:

- `exploring → anxiety`: при словах "боится", "стеснительный", "плачет"
- `exploring → ready_to_buy`: при "запишите", "хочу записать"  
- `exploring → price_sensitive`: при жалобах на цену
- `price_sensitive → ready_to_buy`: ТОЛЬКО при явном принятии ("ладно, запишите")
- `anxiety → exploring`: редко, при смене темы
- **⚠️ ВАЖНО**: Инерция 85% для price_sensitive НЕ реализована! Сигнал определяется заново для каждого сообщения

### Intent Types:

- **success** → Generator создаёт ответ из документов
- **offtopic** → Юмор (33%) или стандартная фраза  
- **need_simplification** → Просьба упростить вопрос (4+ вопроса)

## 🔐 Ключевые алгоритмы

### Выбор документов в Router:
```python
# router.py использует trigger_words из summaries.json
for word in message.lower().split():
    for doc, metadata in summaries.items():
        if word in metadata["trigger_words"]:
            relevant_docs.add(doc)
return list(relevant_docs)[:4]  # Максимум 4
```

### Filter Offtopic (main.py:189-228):
```python
def filter_offtopic_from_history(history):
    filtered = []
    for msg in history:
        # Пропускаем offtopic сообщения
        if msg.get("metadata", {}).get("intent") != "offtopic":
            filtered.append(msg)
    return filtered[-10:]  # Максимум 10 сообщений
```

### CTA Rate Limiting (offers_catalog.py):
```python
last_cta_position[user_id] = message_count
# CTA добавляется если:
# - Прошло минимум 2 сообщения с последнего CTA
# - Подходящий user_signal для CTA
```

## 🔧 Критические фиксы

### 1. HOTFIX сохранения user_signal (main.py:159-179)
```python
# Проблема: Router сбрасывает signal при offtopic
# Решение: Глобальный словарь chat.user_signals_history
```

### 2. Постпроцессинг приветствий (response_generator.py:105-111)
```python
# Проблема: Claude не здоровается в ответ
# Решение: Принудительное добавление "Привет!" если social_context == "greeting"
```

### 3. Обработка mixed интентов (router.py:524-530)
```python
# Проблема: "Добрый день! Расскажите о школе" → offtopic
# Решение: Примеры императивов после приветствий
```

## 📁 Структура проекта

```
src/
├── main.py                 # Оркестратор + HOTFIX user_signal + персистентность
├── router.py               # Gemini роутер (600+ строк промпта!)
├── response_generator.py   # Claude генератор + постпроцессинг
├── persistence_manager.py  # 🆕 Сохранение/восстановление состояний в JSON
├── zhvanetsky_humor.py     # Юмор с блокировками
├── offers_catalog.py       # CTA с rate limiting
└── history_manager.py      # LRU Cache 1000 users × 10 msgs

data/
├── persistent_states/      # 🆕 JSON файлы состояний пользователей
├── documents_compressed/   # 13 документов знаний (сжатые для промпта)
│   ├── conditions.md      # ⭐ ЛОКАЦИЯ: офис Киев, парковка, расписание, техтребования
│   ├── pricing.md         # Цены: 7000-8000 грн/мес, скидки, рассрочка
│   ├── methodology.md     # Игровая методика, soft skills, возрастные группы
│   ├── faq.md            # Частые вопросы родителей
│   ├── courses_detailed.md # 4 курса: Эмоциональный Компас, Капитан Проектов и др.
│   ├── teachers_team.md  # Преподаватели с психологическим образованием
│   └── [ещё 7 файлов]    # Результаты, безопасность, ROI, партнёры и др.
└── summaries.json         # Метаданные для Router: trigger_words, typical_questions
    Структура:
    {
      "conditions.md": {
        "trigger_words": ["расписание", "парковка", "адрес", "офис", "метро"],
        "typical_questions": ["Где находится офис?", "Есть ли парковка?"]
      }
    }

test_dialogues_v2.json     # 10 тестовых диалогов  
http_sandbox_universal.py  # Универсальная песочница
```

## 🧪 Тестирование

### Инструменты:

| Скрипт | Назначение | Использование |
|--------|------------|---------------|
| `http_sandbox_universal.py` | Полные диалоги | `python http_sandbox_universal.py dialog_v2_1` |
| `http_sandbox_simple.py` | Старые тесты | `python http_sandbox_simple.py dialog_5` |
| `curl` | Одиночные запросы | См. Quick Start |

### Ключевые тест-кейсы:

1. **dialog_v2_1** - Мама-методист (exploring → ready_to_buy, парковка)
2. **dialog_v2_3** - Тревожная бабушка (anxiety, без юмора)
3. **dialog_v2_4** - Скептик-эконом (price_sensitive, блокировка)
4. **dialog_v2_10** - Эмоциональные качели (переходы сигналов)
5. **burned_mom** - Мама с негативным опытом (восстановление доверия)
6. **comparing_dad** - Папа-аналитик (методичное сравнение школ)

## 📈 История версий

### v0.13.3 (04.09.2025) - Код ревью и план критических исправлений
- **Проведён детальный код ревью** всей системы
- **Выявлены 5 критических проблем** для production:
  - Отсутствие HTTP timeout (может заблокировать сервер)
  - Нет проверки ENV переменных при старте
  - Потеря user_signal при рестарте
  - Отсутствие retry механизма для LLM
  - Кеширование Gemini работает неправильно
- **Создан детальный план исправлений** `MVP_CRITICAL_FIXES_PLAN.md`:
  - 7 основных фиксов + 2 оптимизации
  - Расписан на 7-9 часов работы
  - Включает готовый код и инструкции тестирования
- **Приоритизированы задачи** для MVP vs Production
- **Результат:** Чёткий план действий для стабилизации MVP

### v0.13.2 (04.09.2025) - Унификация доменов и удаление артефактов
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ:** Унификация всех доменных ссылок на ukido.com.ua
- **response_generator.py:120-124** - автоматическая замена ukido.ua на ukido.com.ua:
  - Решена проблема дублирования доменов в CTA и контактах
  - Все ссылки теперь ведут на единый домен ukido.com.ua
  - Система проверяет существующие контакты перед добавлением новых
- **response_generator.py:734** - удаление числовых артефактов:
  - Regex `r'\d+-[а-я]+\d{2,}'` удаляет артефакты типа "30-секундное00"
  - Сохранены корректные временные выражения ("15-20 минут", "30-секундное")
- **response_generator.py:147-153** - умная проверка существующих контактов:
  - Система ищет упоминания "ukido", "+380", "запишитесь" перед добавлением
  - Предотвращает дублирование CTA и контактной информации
- **Новые тестовые сценарии**: burned_mom и comparing_dad
  - test_burned_parent.json содержит 2 реалистичных сценария
  - Проверка восстановления доверия после негативного опыта
  - Валидация методичного подхода к выбору школы
- **Результат:** Устранены все артефакты, унифицированы домены, улучшена логика CTA
- **Тестирование:** Созданы полные отчёты burned_mom_20250904_150122.md и comparing_dad_20250904_150315.md

### v0.13.1 (04.09.2025) - Восстановление органичной интеграции CTA
- **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ:** Восстановлена органичная интеграция CTA (было 25%, стало 100%)
- **response_generator.py** - усиление императивных инструкций для CTA:
  - Возвращены CAPS императивы (КРИТИЧНО, ОБЯЗАТЕЛЬНО) 
  - CTA инструкция перемещена в конец user message для максимального приоритета
  - Дифференцированная позиция CTA по сигналам (ready_to_buy/price_sensitive → начало, другие → конец)
  - Добавлен явный запрет на дублирование ("НЕ ДУБЛИРУЙ", "ОДИН РАЗ")
  - Обновлены few-shot примеры без повторений CTA
- **response_generator.py:18-29** - исправлен критический баг с несуществующей переменной config:
  - cfg сохраняется как self.cfg для доступа из всех методов класса
  - Безопасное обращение через getattr с fallback на значения по умолчанию
- **response_generator.py:950-992** - улучшенная верификация CTA:
  - Менее строгая проверка по концептам, не точным фразам
  - Диагностическое логирование успеха/провала интеграции
- **Результат:** CTA органично встраиваются в 100% случаев без дублирования
- **Урок:** Позиция инструкции важнее формулировки (recency bias в LLM)
- **Документация:** См. CTA_ORGANIC_FIX_PLAN.md для деталей проблемы и решения

### v0.13.0 (03.09.2025) - Production-ready оптимизация
- **response_generator.py** - рефакторинг промпта Claude:
  - Убраны 17 конкретных вариантов начал фраз (-39 строк)
  - Объединены правила о фактах в "ЖЕЛЕЗНЫЕ ПРАВИЛА" (-10 строк)
  - Упрощена структура ответа (-8 строк)
  - Сокращена price_sensitive логика с 46 до 9 строк
  - Итого: промпт сокращён на 25% без потери функциональности
- **router.py** - оптимизация промпта Gemini:
  - Консолидированы правила КРИТИКА = ВОПРОС
  - Добавлены правила для скептиков и возражений
  - Исправлена классификация эмоциональных восклицаний
  - Размер увеличился на 8% из-за новых примеров, но улучшена структура
- **response_generator.py:24** - исправлен критический баг с обрезанием ответов:
  - max_tokens увеличен с 400 до 1200 (из конфига MAX_TOKENS_ANSWER)
  - Добавлена защита от обрезанных ответов в постпроцессинге
  - Проблема: ответы обрезались на полуслове типа "предупредить ку"
- **response_generator.py:224-228** - усилена защита от галлюцинаций:
  - Явный запрет на выдумывание API и сроков разработки
  - Готовый ответ для технических вопросов про интеграции
  - Проблема: Claude выдумывал "API в разработке через 3-4 месяца"
- **test_tech_dad.json** - добавлен тест технического папы-программиста
  - Проверка галлюцинаций про технические детали
  - Выявлено: 80% информации из документов, 20% выдумано (API, сроки)
- **Результат:** Система готова к production с оптимизированными промптами

### v0.12.25 (02.09.2025) - Критические исправления для диалогов
- **router.py:506-510** - исправлена классификация эмоциональных восклицаний:
  - "Интересно!", "Здорово!" больше не считаются приветствиями
  - Решена проблема повторных приветствий в середине диалога
- **router.py:389-473** - улучшена обработка скептицизма:
  - Критика и возражения теперь success с правильными документами
  - "Не работает", "не нужно" → success + релевантные документы
- **response_generator.py:546-560** - добавлен словарь замен английских слов:
  - "empathy" → "эмпатичными", "soft skills" → "гибкие навыки"
  - Решена проблема английских слов в русских ответах

### v0.12.24 (02.09.2025) - Исправление противоречивых данных о методисте
- **teachers_team.md:85** - исправлено имя методиста:
  - Было: "ОЛЬГА МИРНАЯ - Методист" 
  - Стало: "ЕЛЕНА МИРОШНИКОВА - Главный методист"
  - Согласовано с mission_values_history.md:53
- **summaries.json** - обновлены все упоминания методиста:
  - core_topics: "Ольга Мирная (методист)" → "Елена Мирошникова (главный методист)"
  - key_facts: исправлены имена для согласованности
  - trigger_words: убраны "Ольга", "Мирная", добавлена "Мирошникова"
- **Проблема:** Claude "галлюцинировал" имя методиста, но на самом деле использовал противоречивые данные из документов
- **Решение:** Устранены противоречия в исходных документах, везде указано единое имя
- **Результат:** Claude больше не будет путать имя методиста, исчезли лишние приветствия в середине диалога

### v0.12.23 (02.09.2025) - Улучшенное понимание формата обучения
- **summaries.json** - добавлены trigger_words для распознавания транспортных вопросов:
  - Добавлены слова: "забирать", "привозить", "довозить", "везти", "отвозить" и их формы
  - Эффект: Router теперь включает conditions.md при упоминании транспорта
- **router.py** - добавлены примеры implicit_questions для транспортных контекстов:
  - "После работы забирать буду" → implicit: ["Нужно ли приезжать за ребёнком?"]
  - Система теперь понимает скрытые вопросы о необходимости физического присутствия
- **response_generator.py** - умный постпроцессинг для прояснения онлайн-формата:
  - Органичная вставка уточнений про онлайн рядом с информацией о времени
  - Контекстно-зависимые формулировки для разных ситуаций
  - Fallback для случаев, когда нет упоминания времени
- **Проблема:** Родители не понимали, что обучение онлайн (фраза "забирать буду")
- **Решение:** Система теперь проактивно прояснит формат при упоминании транспорта
- **Результат:** Естественное прояснение преимуществ онлайн-обучения

### v0.12.22 (02.09.2025) - Исправление видимых CTA маркеров
- **response_generator.py** - удалены служебные маркеры из ответов пользователям:
  - Метод `generate` (строка 124) - убрано добавление маркера к финальному тексту
  - Метод `_inject_offer` (строка 688) - убрано добавление маркера [CTA_PRICE] в текст
  - Метод `_get_cta_marker` (строка 834) - возвращает пустую строку вместо HTML комментария
- **Проблема:** HTML комментарии типа `<!-- [CTA_PRICE] -->` были видны в ответах бота
- **Решение:** Маркеры больше не добавляются в текст ответа, остаются только в метаданных
- **Результат:** Чистые ответы без служебной информации для пользователей

### v0.12.21 (02.09.2025) - Улучшение естественности статистики
- **documents_compressed/** - обновлены статистические данные для естественности:
  - 542 → "Около 540", 156 → "Около 160", 89 → "Почти 90", 67 → "Более 70"
  - "85% становятся лидерами" → "Больше половины становятся лидерами"
  - "на 0.8 балла" → просто "улучшается"
  - Вариативность формулировок: "около", "почти", "более", "свыше"
- **Анализ температуры** - сохранена T=0.1 для минимизации галлюцинаций в RAG
- **Тестирование** - протестированы dialog_v2_5 (IT-мама) и dialog_v2_6 (Уставшая мама):
  - ✅ Система корректно работает с техническими терминами
  - ⚠️ Выявлена сверхчувствительность anxiety триггеров
  - ✅ Отлично показывает альтернативы при price_sensitive
- **Результат:** Более естественные ответы без риска галлюцинаций

### v0.12.20 (01.09.2025) - Персистентность состояний
- **persistence_manager.py** - новый модуль для сохранения/восстановления состояний
  - Сохранение в JSON файлы после каждого сообщения
  - Автоматическая загрузка при старте сервера
  - Очистка файлов старше 7 дней
  - LRU на 10,000 файлов максимум
- **main.py:95-109** - интеграция PersistenceManager
  - Загрузка сохранённых состояний при старте
  - Сохранение после каждого сообщения (строки 444-450)
  - Graceful shutdown с сохранением всех активных состояний
- **main.py:111-149** - обработчики SIGTERM/SIGINT для graceful shutdown
  - Сохранение всех состояний перед остановкой
  - Регистрация через signal и atexit
- **Структура данных:**
  ```
  data/persistent_states/{user_id}.json
  {
    "history": [...],           # До 10 последних сообщений
    "user_signal": "...",       # Последний сигнал пользователя  
    "greeting_exchanged": bool, # Поздоровались ли
    "message_count": int,       # Счётчик сообщений
    "last_updated": "ISO8601"   # Временная метка
  }
  ```
- **Результат:** Диалоги сохраняются при рестарте сервера!

### v0.12.19 (31.08.2025) - Вариативность и анализ инерции
- **response_generator.py:192-205** - добавлена вариативность для price_sensitive
  - 7 разных вариантов начала ответа (как для anxiety_about_child)
  - Инструкция НЕ использовать "Хорошая новость" больше 1 раза
  - Обновлены few-shot примеры для разнообразия
- **http_sandbox_universal.py:117** - генерация уникальных user_id для тестов
  - Решает проблему с повторными приветствиями в тестах
  - Обход 12-часового TTL в SocialStateManager
- **Анализ инерции**: Выявлено, что инерция 85% для price_sensitive НЕ реализована
  - HOTFIX работает только для offtopic сообщений  
  - Для success сообщений сигнал определяется заново
  - Решение: оставить как есть для MVP (более точные ответы)
- **Результат:** Убрали навязчивое "Хорошая новость", улучшили вариативность

### v0.12.18 (29.08.2025) - Локация и парковка
- **conditions.md:30-41** - добавлен раздел "Формат обучения и офис"
  - Офис: Киев, ул. Саксаганского 121, офис 305
  - Метро Университет, автобусы №14,18,114
  - Парковка: 5 бесплатных мест + платная на улице
  - Подчёркнуто: обучение 100% онлайн
- **summaries.json** - добавлены trigger_words для локации
- **response_generator.py:105-111** - усилен постпроцессинг приветствий
- **Результат:** Вопросы про парковку теперь success, а не offtopic

### v0.12.17 (28.08.2025) - Mixed интенты
- **router.py:524-530** - исправлены императивы после приветствий
- **Результат:** "Добрый день! Расскажите о школе" → success

### v0.12.16 (28.08.2025) - Hallucination fix
- **conditions.md:78-152** - процедуры после действий пользователя
- **summaries.json:108-157** - trigger_words для завершённых действий
- **Результат:** Нет выдумывания после оплаты/записи

## ⚙️ Конфигурация

```python
# src/config.py
MODEL_ROUTER = "google/gemini-2.5-flash"
MODEL_ANSWER = "anthropic/claude-3.5-haiku"  
ZHVANETSKY_PROBABILITY = 0.33  # Вероятность юмора
MAX_HISTORY_SIZE = 10           # Сообщений на пользователя
CACHE_SIZE = 1000               # LRU пользователей
DETERMINISTIC_MODE = False      # True для воспроизводимости
```

## 🚨 Известные проблемы

### КРИТИЧЕСКИЕ (требуют немедленного исправления):
1. **Отсутствие HTTP timeout** - зависший запрос блокирует сервер ⚡ 2 мин фикс
2. **Нет проверки ENV при старте** - сервер запускается без API ключа ⚡ 2 мин фикс
3. **Потеря user_signal при рестарте** - теряется контекст price_sensitive → агрессивные CTA
4. **Нет retry для LLM** - 30% пользователей уходят при timeout
5. **Кеширование Gemini НЕ работает** - кешируется вся история вместо статичной части

### Некритичные для MVP:
1. **Инерция price_sensitive не работает** - сигнал сбрасывается между сообщениями
2. **Сверхчувствительность anxiety триггеров** - "ребёнок доведёт" → anxiety_about_child
3. **Неуместные CTA для завершённых действий** - предлагаем записаться тем, кто уже оплатил
4. **Игнорирование отказов** - продолжаем предлагать CTA после "не интересно"
5. **Повторение одинаковых CTA** - отсутствует вариативность формулировок

## ✅ Исправленные проблемы

1. ~~**Потеря контекста при рестарте**~~ - ИСПРАВЛЕНО в v0.12.20 через персистентность
2. ~~**Приветствия только для новых user_id**~~ - ИСПРАВЛЕНО через уникальные user_id в тестах
3. ~~**Слишком точная статистика**~~ - ИСПРАВЛЕНО в v0.12.21 округлением и вариативностью
4. ~~**Отсутствие альтернатив при price_sensitive**~~ - ИСПРАВЛЕНО, система предлагает дешевле курсы
5. ~~**"Галлюцинации" с именем методиста**~~ - ИСПРАВЛЕНО в v0.12.24 устранением противоречий в документах
6. ~~**Лишние приветствия в середине диалога**~~ - ИСПРАВЛЕНО в v0.12.24 как побочный эффект

## 💡 Best Practices

### При добавлении информации:
1. **НЕ создавай новые документы** - добавляй в существующие
2. **Обновляй summaries.json** - trigger_words критичны для Router
3. **Тестируй через http_sandbox_universal.py** - полный контекст

### При отладке:
1. **Проверяй intent и signal** в ответе API
2. **Смотри логи сервера** - там видны все этапы
3. **Используй DETERMINISTIC_MODE** для воспроизводимости

### Постпроцессинг vs Промпты:
- **Используй постпроцессинг** для детерминированных правил
- **Не борись с LLM** - проще исправить результат
- **Пример:** Приветствия добавляются постпроцессингом

## 📞 Контакты и поддержка

- **GitHub Issues:** https://github.com/anthropics/claude-code/issues
- **Основная ветка:** main
- **Ветка с юмором:** feature/zhvanetsky-humor

---

**Статус:** MVP требует критических исправлений (см. MVP_CRITICAL_FIXES_PLAN.md)  
**Последнее обновление:** 04.09.2025  
**Версия:** 0.13.3  
**Следующие шаги:** Начать с Fix #0.1 и #0.2 (5 минут работы), затем основные фиксы

### Оставшиеся улучшения для v1.0:
1. **Контекст множественных детей** - система не запоминает "у меня двое: 9 и 12 лет"
2. ~~**Персистентная память**~~ - ✅ РЕАЛИЗОВАНО в v0.12.20
3. **Сравнение курсов** - показывает альтернативы при price_sensitive ✅ v0.12.21
4. **Инерция сигналов** - price_sensitive должен держаться дольше
5. **Чувствительность anxiety триггеров** - слишком легко срабатывает на "ребёнок доведёт"

### Архитектурные улучшения (2025 roadmap):
1. **Multi-agent система** вместо монолитного Router
2. **Knowledge Graph** вместо линейных документов  
3. **Hybrid retrieval** (векторы + BM25 + граф)
4. **Agentic RAG** с памятью и оркестрацией

---

**Статус:** Production Ready (с согласованными данными)  
**Последнее обновление:** 02.09.2025  
**Версия:** 0.12.24