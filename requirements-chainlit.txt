# Минимальный набор зависимостей для Chainlit
# Chainlit сам установит совместимые версии всех остальных пакетов

# Основные зависимости проекта
python-dotenv==1.0.0
httpx==0.26.0
markdown==3.6
pydantic==2.5.3

# Chainlit - пусть сам разрешит все зависимости
chainlit==1.3.0